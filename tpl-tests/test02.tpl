/$%%% this is second test file$//
void doMath(int a, int b, float val c) {
	int add = a + b +c;
	float mult = a *b * c;
	int div = a/ (b / c);
return add + mult;
}

float count(int a)
{
 while (b == true)
{ 
     str name = "haBiB"; 
     float a = a + 0.01E;
      if (a >= 9.25 and name == "HABIB" ) {
         for (int i = 100; i >= 0; i = i -1) {
               a = a^i;
         }
     } 
     else {
          break;
     }
}	
			